# Projeto: _exemplo

> Copie esta pasta para criar um projeto de verdade.
> Teto: 150 linhas. Passou disso, o worker abre uma sub-task de compactação.

## O que é

(uma frase: o que esse projeto faz e para quem)

## Stack

Stack 1 (Flask + Jinja2 + HTMX + design system AM).
Template base: `template-stack1-flask-htmx`.

## Como rodar

    python -m venv .venv && source .venv/bin/activate
    pip install -r requirements.txt
    cp .env.example .env
    python app.py

## Como validar

    bash check.sh

## O que NÃO tocar

- (módulos, arquivos ou áreas que exigem conversa antes)

## Responsáveis

- dono: (nome)
- revisor de PR: (nome)

# G1 — O F1 do n8n enviando por amknowledge@andrademaia.com

## 1. Objetivo
Depois de você terminar, o fluxo `esteira-comms-out` envia sempre pela
caixa `amknowledge@andrademaia.com`, e o `COMO-IMPORTAR.md` diz o que
configurar à mão para isso valer.

## 2. Caminhos absolutos
- editar: `/home/nicolas/Área de trabalho/esteira/n8n/esteira-comms-out.json`
- editar: `/home/nicolas/Área de trabalho/esteira/n8n/COMO-IMPORTAR.md`
- **A PROVA:** `/home/nicolas/Área de trabalho/esteira/orquestracao/provas/g1.sh`
- leia: `/home/nicolas/Área de trabalho/esteira/n8n/README.md` (a especificação de F1)
- leia: `/home/nicolas/Área de trabalho/esteira/esteira/comm.py` (o envelope que o F1 recebe)
- n8n rodando: `http://localhost:5678`; binário em
  `/home/nicolas/.nvm/versions/node/v24.18.0/bin/n8n`

## 3. Escopo
Só `n8n/esteira-comms-out.json` e `n8n/COMO-IMPORTAR.md`.
Qualquer arquivo fora disso é violação. **Não escreva nada fora do repo.**

## 4. O que NÃO tocar
`n8n/esteira-comms-in.json` (é o F2, outra frente) · `refs/n8n/**` (são as
referências sanitizadas) · `esteira/**` · `orquestracao/provas/**` ·
qualquer `.env` · o banco do n8n em `~/.n8n`.

**E não ative nenhum fluxo.** Ativar fluxo n8n manda e-mail de verdade, e é
proibido pelo papel do orquestrador. Os fluxos entram inativos.

## 5. Prova exigida

    cd "/home/nicolas/Área de trabalho/esteira"
    bash orquestracao/provas/g1.sh ; echo "saida=$?"

Tem que sair **0**. Cole a saída inteira.

## 6. Relatório
`/home/nicolas/Área de trabalho/esteira/orquestracao/RELATORIO-G1.md`: o que fez, o que NÃO fez, o que assumiu,
o que quebrou.

## 7. Detalhes que importam
- Isto é **troca de remetente e de credencial nos nós**, não fluxo novo. O
  `COMO-IMPORTAR.md` já lista o que é placeholder — hoje o endereço da
  equipe está como `equipe@CONFIGURAR.invalid`. Substitua pelo real e
  documente que a credencial `Outlook — Esteira` precisa apontar para a
  caixa `amknowledge@andrademaia.com`.
- **Credencial nunca no JSON.** Referência por nome pode e deve ficar — é o
  que ensina o formato a quem for configurar. Valor, nunca. A prova reprova.
- **Não quebre o que já está provado.** A prova verifica que continuam de pé:
  a linha `--- responda acima desta linha ---` (sem ela o F2 recebe a
  *thread* citada inteira como se fosse instrução) e a regra dura
  `to !== 'team'` (o agente nunca fala com o demandante; qualquer outro
  destinatário cai na rota 6, `Recusar envio proibido`).
- O JSON tem que continuar **importando**: a prova roda
  `n8n import:workflow` e exige saída 0. O `refs/README.md` avisa que fluxo
  n8n editado à mão costuma parar de abrir — confira antes de dizer pronto.
- PT-BR em nome de nó e mensagem.
